# House Robber

**Platform:** LeetCode
**Link:** https://leetcode.com/problems/house-robber/
**Language:** C++
**Difficulty:** Medium
**Topic Tags:** `Array`, `Dynamic Programming`

    ## 💡 Intuition

The core constraint is that adjacent houses cannot be robbed, leading to a choice at each house: either rob it and skip the next, or don't rob it and consider the next. This structure suggests a dynamic programming approach to find the optimal sum.

## 🧩 Approach

This solution uses a top-down dynamic programming (memoization) approach. A recursive function `f(ind)` calculates the maximum money that can be robbed starting from house `ind`. At each house `ind`, it considers two options: either rob `nums[ind]` and add it to the result of `f(ind+2)` (skipping the next house), or don't rob `nums[ind]` and take the result of `f(ind+1)`. The maximum of these two options is stored in a DP table to avoid recomputation, with base cases handling out-of-bounds indices.

## ⏱️ Complexity

- **Time:** O(n), because each of the 'n' states (indices) in the DP table is computed exactly once.
- **Space:** O(n), due to the DP array storing results for 'n' states and the recursion stack depth in the worst case.

## ⚠️ Edge Cases

The solution correctly handles small input sizes like `nums.length = 1` or `nums.length = 2` by its base cases and recursive structure, returning the single house's money or the maximum of two non-adjacent houses respectively.
## Problem Statement

You are a professional robber planning to rob houses along a street. Each house has a certain amount of money stashed, the only constraint stopping you from robbing each of them is that adjacent houses have security systems connected and it will automatically contact the police if two adjacent houses were broken into on the same night.

Given an integer array nums representing the amount of money of each house, return the maximum amount of money you can rob tonight without alerting the police.

 
Example 1:

Input: nums = [1,2,3,1]
Output: 4
Explanation: Rob house 1 (money = 1) and then rob house 3 (money = 3).
Total amount you can rob = 1 + 3 = 4.


Example 2:

Input: nums = [2,7,9,3,1]
Output: 12
Explanation: Rob house 1 (money = 2), rob house 3 (money = 9) and rob house 5 (money = 1).
Total amount you can rob = 2 + 9 + 1 = 12.


 
Constraints:


	1 <= nums.length <= 100
	0 <= nums[i] <= 400

## Stats (from LeetCode)

- **runtime:** 0 ms
- **runtimePercentile:** 100.0%
- **memory:** 10.6 MB
- **memoryPercentile:** 46.6%

---
_Auto-generated by DSA AutoCommit._
